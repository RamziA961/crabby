import { TGlobalAction } from "./action";
import { TGlobalState } from "./initial_state";


export function GlobalReducer(state: TGlobalState, action: TGlobalAction) : TGlobalState {
    switch(action) {

    }

    return state;
}
